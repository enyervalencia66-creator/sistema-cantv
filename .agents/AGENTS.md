# Reglas del Proyecto

- Después de realizar modificaciones al código del sistema, ejecuta siempre de forma proactiva `git add`, `git commit` y `git push` para guardar y subir los cambios al servidor. No necesitas pedir permiso para esto.
